org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# Built-in Kotlin is required by modern plugins (e.g. file_picker) that, on
# AGP 9+, stop applying the Kotlin Gradle Plugin and rely on AGP compiling their
# Kotlin sources. Enabling it lets those plugins build; plugins that still apply
# KGP (e.g. audio_waveforms) continue to work with a deprecation warning.
android.builtInKotlin=true
